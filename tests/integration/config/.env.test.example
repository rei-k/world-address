# =============================================================================
# Integration Test Environment Variables
# =============================================================================
# このファイルをコピーして .env.test を作成し、実際の値を設定してください
# Copy this file to .env.test and fill in actual values
#
# ⚠️ 重要 / IMPORTANT:
# .env.test ファイルは .gitignore に含まれており、コミットされません
# The .env.test file is in .gitignore and will not be committed
# =============================================================================

# =============================================================================
# PostgreSQL
# =============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=vey_test
POSTGRES_USER=vey_user
POSTGRES_PASSWORD=your_password
POSTGRES_SSL=false

# PostgreSQL接続文字列 / Connection string (alternative)
# POSTGRES_URL=postgresql://vey_user:your_password@localhost:5432/vey_test

# =============================================================================
# MongoDB
# =============================================================================
MONGODB_URI=mongodb://localhost:27017/vey_test
MONGODB_DB=vey_test

# MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vey_test

# =============================================================================
# MySQL / MariaDB
# =============================================================================
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=vey_test
MYSQL_USER=vey_user
MYSQL_PASSWORD=your_password

# =============================================================================
# SQLite (ローカル開発用 / For local development)
# =============================================================================
SQLITE_DB_PATH=./tests/integration/data/vey_test.db

# =============================================================================
# Firebase
# =============================================================================
FIREBASE_API_KEY=your_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id

# Firebase Admin SDK (サービスアカウント / Service Account)
FIREBASE_SERVICE_ACCOUNT_KEY=path/to/service-account-key.json

# または環境変数として JSON を設定 / Or set JSON as environment variable
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# =============================================================================
# Supabase
# =============================================================================
SUPABASE_URL=https://your_project.supabase.co
SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Supabase Database (direct PostgreSQL connection)
# SUPABASE_DB_HOST=db.your_project.supabase.co
# SUPABASE_DB_PORT=5432
# SUPABASE_DB_NAME=postgres
# SUPABASE_DB_USER=postgres
# SUPABASE_DB_PASSWORD=your_db_password

# =============================================================================
# AWS Services
# =============================================================================
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key

# S3
AWS_S3_BUCKET=vey-test-bucket

# DynamoDB
AWS_DYNAMODB_TABLE=vey_test_table

# RDS (PostgreSQL/MySQL)
# AWS_RDS_HOST=your_rds_instance.region.rds.amazonaws.com
# AWS_RDS_PORT=5432
# AWS_RDS_DB=vey_test
# AWS_RDS_USER=admin
# AWS_RDS_PASSWORD=your_password

# =============================================================================
# Google Cloud Platform (GCP)
# =============================================================================
GCP_PROJECT_ID=your_project_id
GCP_CREDENTIALS=path/to/credentials.json

# または環境変数として JSON を設定 / Or set JSON as environment variable
# GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json

# Cloud Storage
GCP_STORAGE_BUCKET=vey-test-bucket

# Cloud Firestore
GCP_FIRESTORE_DB=(default)

# Cloud SQL (PostgreSQL/MySQL)
# GCP_SQL_INSTANCE=your_project:region:instance_name
# GCP_SQL_DB=vey_test
# GCP_SQL_USER=vey_user
# GCP_SQL_PASSWORD=your_password

# =============================================================================
# Azure Services
# =============================================================================
# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your_account;AccountKey=your_key;EndpointSuffix=core.windows.net
AZURE_STORAGE_CONTAINER=vey-test-container

# Azure Cosmos DB
AZURE_COSMOS_DB_ENDPOINT=https://your_account.documents.azure.com:443/
AZURE_COSMOS_DB_KEY=your_key
AZURE_COSMOS_DB_DATABASE=vey_test

# Azure Database for PostgreSQL
# AZURE_POSTGRES_HOST=your_server.postgres.database.azure.com
# AZURE_POSTGRES_PORT=5432
# AZURE_POSTGRES_DB=vey_test
# AZURE_POSTGRES_USER=admin@your_server
# AZURE_POSTGRES_PASSWORD=your_password
# AZURE_POSTGRES_SSL=true

# =============================================================================
# Redis (キャッシング / Caching)
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Redis Cloud
# REDIS_URL=redis://username:password@host:port

# =============================================================================
# テスト設定 / Test Configuration
# =============================================================================
# テストタイムアウト (ミリ秒 / milliseconds)
TEST_TIMEOUT=30000

# 並列実行数 / Parallel execution count
TEST_CONCURRENCY=1

# クリーンアップを有効化 / Enable cleanup
TEST_CLEANUP=true

# ログレベル / Log level (debug, info, warn, error)
TEST_LOG_LEVEL=info

# =============================================================================
# 開発環境 / Development Environment
# =============================================================================
NODE_ENV=test
