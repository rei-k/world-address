# Vey エコシステム セキュリティアーキテクチャ / Security Architecture

このドキュメントは、Veyエコシステムのセキュリティアーキテクチャを詳細に示します。

This document illustrates the detailed security architecture of the Vey ecosystem.

---

## 🔐 セキュリティレイヤー / Security Layers

### 多層防御アーキテクチャ / Defense in Depth Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                  Layer 7: Compliance & Governance               │
│  • GDPR  • CCPA  • PCI DSS  • SOC 2  • ISO 27001                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                  Layer 6: Monitoring & Auditing                 │
│  • SIEM  • Log Analysis  • Anomaly Detection  • Audit Trail     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                  Layer 5: Data Protection                       │
│  • E2EE  • ZKP  • Data Masking  • Tokenization                  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                  Layer 4: Application Security                  │
│  • Input Validation  • SQL Injection Prevention  • XSS防止      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                  Layer 3: API Security                          │
│  • Rate Limiting  • API Keys  • OAuth 2.0  • JWT                │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                  Layer 2: Network Security                      │
│  • WAF  • DDoS Protection  • TLS 1.3  • mTLS                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                  Layer 1: Infrastructure Security               │
│  • Firewall  • VPC  • Security Groups  • IAM                    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔑 認証・認可 / Authentication & Authorization

### 認証フロー / Authentication Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                    Multi-Factor Authentication                   │
└──────────────────────────────────────────────────────────────────┘

Step 1: Primary Authentication
┌──────────────┐
│    User      │
└──────┬───────┘
       │ Login Attempt
       ▼
┌──────────────────────────────────────────────────────────────────┐
│              Auth Service                                        │
│                                                                  │
│  Authentication Methods:                                         │
│  ┌────────────────────────────────────────────────┐             │
│  │ 1. Username/Password                           │             │
│  │    • bcrypt hashing (cost: 12)                 │             │
│  │    • Salted                                    │             │
│  │                                                │             │
│  │ 2. Social Login                                │             │
│  │    • Google OAuth 2.0                          │             │
│  │    • Apple Sign In                             │             │
│  │    • LINE Login                                │             │
│  │                                                │             │
│  │ 3. Biometric (Mobile)                          │             │
│  │    • Face ID / Touch ID                        │             │
│  │    • Fingerprint                               │             │
│  └────────────────────────────────────────────────┘             │
└──────┬───────────────────────────────────────────────────────────┘
       │ Primary Auth Success
       ▼

Step 2: Multi-Factor Authentication (MFA)
┌──────────────────────────────────────────────────────────────────┐
│              MFA Methods                                         │
│                                                                  │
│  ┌────────────────────────────────────────────────┐             │
│  │ 1. TOTP (Time-based One-Time Password)        │             │
│  │    • Google Authenticator                      │             │
│  │    • Authy                                     │             │
│  │    • 30-second window                          │             │
│  │                                                │             │
│  │ 2. SMS Code                                    │             │
│  │    • 6-digit code                              │             │
│  │    • 5-minute expiration                       │             │
│  │                                                │             │
│  │ 3. Email Code                                  │             │
│  │    • 6-digit code                              │             │
│  │    • 10-minute expiration                      │             │
│  │                                                │             │
│  │ 4. Push Notification                           │             │
│  │    • Mobile app approval                       │             │
│  │    • Device verification                       │             │
│  └────────────────────────────────────────────────┘             │
└──────┬───────────────────────────────────────────────────────────┘
       │ MFA Success
       ▼

Step 3: Token Generation
┌──────────────────────────────────────────────────────────────────┐
│              Token Service                                       │
│                                                                  │
│  JWT Token:                                                      │
│  ┌────────────────────────────────────────────────┐             │
│  │ Header:                                        │             │
│  │ {                                              │             │
│  │   "alg": "RS256",                              │             │
│  │   "typ": "JWT"                                 │             │
│  │ }                                              │             │
│  │                                                │             │
│  │ Payload:                                       │             │
│  │ {                                              │             │
│  │   "sub": "user_123",                           │             │
│  │   "iat": 1701604800,                           │             │
│  │   "exp": 1701608400,                           │             │
│  │   "scope": "address:read order:write",         │             │
│  │   "role": "user"                               │             │
│  │ }                                              │             │
│  │                                                │             │
│  │ Signature:                                     │             │
│  │ RSA-SHA256(Header + Payload, Private Key)     │             │
│  └────────────────────────────────────────────────┘             │
│                                                                  │
│  Refresh Token:                                                  │
│  • Opaque token (random string)                                 │
│  • Stored in secure database                                    │
│  • Long-lived (30 days)                                         │
└──────┬───────────────────────────────────────────────────────────┘
       │ Return Tokens
       ▼
┌──────────────┐
│    Client    │
│              │
│ • Access Token (1 hour)                                          │
│ • Refresh Token (30 days)                                        │
└──────────────┘
```

### 認可 (RBAC) / Authorization (Role-Based Access Control)

```
┌──────────────────────────────────────────────────────────────────┐
│                    Role-Based Access Control                     │
└──────────────────────────────────────────────────────────────────┘

Roles:
┌────────────┬──────────────────────────────────────────────────┐
│ Role       │ Permissions                                      │
├────────────┼──────────────────────────────────────────────────┤
│ Admin      │ • *:*                                            │
│            │   (All resources, all actions)                   │
├────────────┼──────────────────────────────────────────────────┤
│ Merchant   │ • order:read, order:write                        │
│            │ • product:read, product:write                    │
│            │ • delivery:read, delivery:write                  │
│            │ • analytics:read                                 │
├────────────┼──────────────────────────────────────────────────┤
│ Driver     │ • delivery:read, delivery:update                 │
│            │ • location:write                                 │
├────────────┼──────────────────────────────────────────────────┤
│ User       │ • address:read, address:write (own)              │
│            │ • order:read, order:write (own)                  │
│            │ • delivery:read (own)                            │
├────────────┼──────────────────────────────────────────────────┤
│ Guest      │ • public:read                                    │
└────────────┴──────────────────────────────────────────────────┘

Permission Check Flow:
┌──────────────┐
│ API Request  │
└──────┬───────┘
       │ + JWT Token
       ▼
┌──────────────────────────────────────────────────────────────────┐
│              Authorization Middleware                            │
│                                                                  │
│  1. Validate JWT Signature                                       │
│  2. Check Token Expiration                                       │
│  3. Extract User ID & Role                                       │
│  4. Load User Permissions from Cache/DB                          │
│  5. Check Resource Access                                        │
│  6. Check Action Permission                                      │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ├─── Authorized ──────────────►  ┌──────────────┐
       │                               │   Process    │
       │                               │   Request    │
       │                               └──────────────┘
       │
       └─── Unauthorized ────────────►  ┌──────────────┐
                                        │   403        │
                                        │   Forbidden  │
                                        └──────────────┘
```

---

## 🔒 データ暗号化 / Data Encryption

### 保存時の暗号化 / Encryption at Rest

```
┌──────────────────────────────────────────────────────────────────┐
│                  Encryption at Rest Architecture                 │
└──────────────────────────────────────────────────────────────────┘

Application Layer:
┌──────────────────────────────────────────────────────────────────┐
│  Client-Side Encryption (E2EE)                                   │
│                                                                  │
│  User Data ──► AES-256-GCM ──► Encrypted Blob                   │
│                     │                                            │
│                     └── User's Public Key                        │
│                         (Only user has private key)              │
└──────┬───────────────────────────────────────────────────────────┘
       │ Encrypted Data
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Server-Side Encryption                                          │
│                                                                  │
│  Encrypted Blob ──► AES-256-CBC ──► Double-Encrypted            │
│                          │                                       │
│                          └── Master Key (from HSM)               │
└──────┬───────────────────────────────────────────────────────────┘
       │ Double-Encrypted Data
       ▼
┌──────────────────────────────────────────────────────────────────┐
│  Database Encryption                                             │
│                                                                  │
│  • Transparent Data Encryption (TDE)                             │
│  • Column-Level Encryption (for sensitive fields)                │
│  • Encrypted Backups                                             │
│                                                                  │
│  PostgreSQL pgcrypto:                                            │
│  ┌────────────────────────────────────────────────┐             │
│  │ CREATE TABLE addresses (                       │             │
│  │   id UUID PRIMARY KEY,                         │             │
│  │   user_id UUID NOT NULL,                       │             │
│  │   encrypted_data BYTEA NOT NULL,               │             │
│  │   encryption_key_id UUID NOT NULL              │             │
│  │ );                                             │             │
│  │                                                │             │
│  │ INSERT INTO addresses VALUES (                 │             │
│  │   uuid_generate_v4(),                          │             │
│  │   user_id,                                     │             │
│  │   pgp_sym_encrypt(address_data, key),          │             │
│  │   key_id                                       │             │
│  │ );                                             │             │
│  └────────────────────────────────────────────────┘             │
└──────────────────────────────────────────────────────────────────┘

Key Management:
┌──────────────────────────────────────────────────────────────────┐
│              Key Management Service (AWS KMS / GCP KMS)          │
│                                                                  │
│  • Master Keys (HSM-backed)                                      │
│  • Automatic Key Rotation (yearly)                               │
│  • Key Versioning                                                │
│  • Access Logging                                                │
│  • Separation of Duties                                          │
└──────────────────────────────────────────────────────────────────┘
```

### 通信時の暗号化 / Encryption in Transit

```
┌──────────────────────────────────────────────────────────────────┐
│                  Encryption in Transit                           │
└──────────────────────────────────────────────────────────────────┘

TLS 1.3 Configuration:
┌──────────────────────────────────────────────────────────────────┐
│  Cipher Suites (順序):                                           │
│                                                                  │
│  1. TLS_AES_256_GCM_SHA384                                       │
│  2. TLS_CHACHA20_POLY1305_SHA256                                 │
│  3. TLS_AES_128_GCM_SHA256                                       │
│                                                                  │
│  Protocols:                                                      │
│  • TLS 1.3 (Required)                                            │
│  • TLS 1.2 (Fallback, deprecated)                               │
│  • TLS 1.1 and below (Disabled)                                 │
│                                                                  │
│  Certificate:                                                    │
│  • RSA 4096-bit or ECDSA P-256                                   │
│  • Certificate Transparency (CT) Logs                            │
│  • OCSP Stapling                                                 │
│  • Certificate Pinning (mobile apps)                             │
└──────────────────────────────────────────────────────────────────┘

Mutual TLS (mTLS) for Service-to-Service:
┌──────────────┐              ┌──────────────┐
│  Service A   │◄──── mTLS ───►│  Service B   │
│              │              │              │
│ • Client Cert│              │• Server Cert │
│ • Private Key│              │• Verify Client│
└──────────────┘              └──────────────┘
```

---

## 🛡️ ゼロ知識証明 (ZKP) / Zero-Knowledge Proofs

### ZKPアーキテクチャ / ZKP Architecture

```
┌──────────────────────────────────────────────────────────────────┐
│              Zero-Knowledge Proof System                         │
└──────────────────────────────────────────────────────────────────┘

Scenario: 住所証明 (生住所を公開せずに配送)
         Address Proof (Delivery without revealing raw address)

Prover (住所所有者 / Address Owner):
┌──────────────────────────────────────────────────────────────────┐
│  1. Secret Input (Private)                                       │
│     • Raw Address: "東京都渋谷区神宮前1-2-3"                      │
│     • Private Key                                                │
│                                                                  │
│  2. Public Input                                                 │
│     • PID: "JP-13-113-01-T07-B12"                                │
│     • Hash(Address)                                              │
│                                                                  │
│  3. Proof Generation                                             │
│     ┌────────────────────────────────────────────┐              │
│     │ zk-SNARK Circuit:                          │              │
│     │                                            │              │
│     │ assert(                                    │              │
│     │   hash(secret_address) == public_hash &&   │              │
│     │   generatePID(secret_address) == public_pid│              │
│     │ )                                          │              │
│     │                                            │              │
│     │ Proof = prove(circuit, secret, public)     │              │
│     └────────────────────────────────────────────┘              │
│                                                                  │
│  4. Output Proof                                                 │
│     • π (proof) - 1KB サイズ                                     │
│     • No raw address revealed                                    │
└──────┬───────────────────────────────────────────────────────────┘
       │ Send Proof (π)
       ▼

Verifier (配送業者 / Delivery Carrier):
┌──────────────────────────────────────────────────────────────────┐
│  1. Receive Proof                                                │
│     • Proof (π)                                                  │
│     • Public Inputs (PID, Hash)                                  │
│                                                                  │
│  2. Verification                                                 │
│     ┌────────────────────────────────────────────┐              │
│     │ verify(proof, public_inputs) → true/false  │              │
│     │                                            │              │
│     │ if (verify == true):                       │              │
│     │   • Prover knows valid address             │              │
│     │   • PID is correct                         │              │
│     │   • Address is deliverable                 │              │
│     │   (without knowing raw address)            │              │
│     └────────────────────────────────────────────┘              │
│                                                                  │
│  3. Use PID for Routing                                          │
│     • Extract hierarchy: JP → 13 → 113 → 01 → T07 → B12        │
│     • Route to appropriate delivery center                       │
│     • No need for full address                                  │
└──────────────────────────────────────────────────────────────────┘

Benefits:
✅ Privacy: 生住所を公開しない
✅ Verifiable: 配送業者は住所の正当性を確認できる
✅ Efficient: 証明サイズは小さい (1KB)
✅ Fast: 検証は数ミリ秒
```

### ZKP実装例 / ZKP Implementation Example

```typescript
import { groth16 } from 'snarkjs';

// Proof Generation (Prover側)
async function generateAddressProof(
  secretAddress: string,
  privateKey: string
): Promise<ProofData> {
  // 1. PID生成
  const pid = generatePID(secretAddress);
  
  // 2. ハッシュ計算
  const addressHash = sha256(secretAddress);
  
  // 3. 証明生成
  const { proof, publicSignals } = await groth16.fullProve(
    {
      secret_address: secretAddress,
      private_key: privateKey
    },
    'circuits/address_proof.wasm',
    'circuits/address_proof.zkey'
  );
  
  return {
    proof,
    publicInputs: {
      pid,
      addressHash,
      timestamp: Date.now()
    }
  };
}

// Proof Verification (Verifier側)
async function verifyAddressProof(
  proofData: ProofData
): Promise<boolean> {
  const vKey = await loadVerificationKey('circuits/verification_key.json');
  
  const isValid = await groth16.verify(
    vKey,
    proofData.publicInputs,
    proofData.proof
  );
  
  if (isValid) {
    console.log('✅ Proof verified: Address is valid');
    console.log('PID for routing:', proofData.publicInputs.pid);
  } else {
    console.log('❌ Proof verification failed');
  }
  
  return isValid;
}
```

---

## 🚨 セキュリティ監視 / Security Monitoring

### SIEM統合 / SIEM Integration

```
┌──────────────────────────────────────────────────────────────────┐
│              Security Information and Event Management           │
└──────────────────────────────────────────────────────────────────┘

Log Sources:
┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│ Application │ │   API GW    │ │  Database   │ │   System    │
│    Logs     │ │    Logs     │ │    Logs     │ │    Logs     │
└──────┬──────┘ └──────┬──────┘ └──────┬──────┘ └──────┬──────┘
       │               │               │               │
       └───────────────┴───────────────┴───────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│              Log Aggregation (Fluentd / Logstash)                │
│                                                                  │
│  • Parse logs                                                    │
│  • Normalize format                                              │
│  • Enrich with metadata                                          │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│              SIEM Platform (Splunk / ELK Stack)                  │
│                                                                  │
│  Detection Rules:                                                │
│  ┌────────────────────────────────────────────────┐             │
│  │ 1. Multiple Failed Login Attempts              │             │
│  │    • Threshold: 5 failures in 5 minutes        │             │
│  │    • Action: Account lock + Alert              │             │
│  │                                                │             │
│  │ 2. Unusual API Access Pattern                  │             │
│  │    • ML-based anomaly detection                │             │
│  │    • Action: Alert security team               │             │
│  │                                                │             │
│  │ 3. Privilege Escalation Attempt                │             │
│  │    • User tries to access admin endpoints      │             │
│  │    • Action: Block + Immediate alert           │             │
│  │                                                │             │
│  │ 4. Data Exfiltration                           │             │
│  │    • Large data download                       │             │
│  │    • Action: Rate limit + Alert                │             │
│  │                                                │             │
│  │ 5. SQL Injection / XSS Attempts                │             │
│  │    • Pattern matching on inputs                │             │
│  │    • Action: Block + Log                       │             │
│  └────────────────────────────────────────────────┘             │
└──────┬───────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────────┐
│              Incident Response                                   │
│                                                                  │
│  Severity Levels:                                                │
│  • Critical: Immediate response (15 min)                         │
│  • High: Response within 1 hour                                  │
│  • Medium: Response within 4 hours                               │
│  • Low: Response within 24 hours                                 │
│                                                                  │
│  Alerts:                                                         │
│  • PagerDuty                                                     │
│  • Slack                                                         │
│  • Email                                                         │
│  • SMS (Critical only)                                           │
└──────────────────────────────────────────────────────────────────┘
```

### 侵入検知 / Intrusion Detection

```
┌──────────────────────────────────────────────────────────────────┐
│              Intrusion Detection System (IDS)                    │
└──────────────────────────────────────────────────────────────────┘

Network-based IDS (NIDS):
┌──────────────────────────────────────────────────────────────────┐
│  • Snort / Suricata                                              │
│  • Monitor network traffic                                       │
│  • Pattern matching against attack signatures                    │
│  • Protocol anomaly detection                                    │
└──────────────────────────────────────────────────────────────────┘

Host-based IDS (HIDS):
┌──────────────────────────────────────────────────────────────────┐
│  • OSSEC / Wazuh                                                 │
│  • File integrity monitoring                                     │
│  • Log analysis                                                  │
│  • Rootkit detection                                             │
└──────────────────────────────────────────────────────────────────┘

Detection Scenarios:
1. Port Scanning
   → Block source IP
   → Alert security team

2. DDoS Attack
   → Rate limiting
   → CloudFlare DDoS protection
   → Geo-blocking if needed

3. Malware Upload
   → File scanning (ClamAV)
   → Quarantine suspicious files
   → Alert + Analysis

4. Brute Force Attack
   → Account lockout
   → CAPTCHA challenge
   → IP blacklisting
```

---

## 📋 コンプライアンス / Compliance

### GDPR対応 / GDPR Compliance

```
┌──────────────────────────────────────────────────────────────────┐
│              GDPR Compliance Framework                           │
└──────────────────────────────────────────────────────────────────┘

1. Data Subject Rights:
   ┌────────────────────────────────────────────────┐
   │ • Right to Access                              │
   │   GET /api/user/data                           │
   │                                                │
   │ • Right to Rectification                       │
   │   PUT /api/user/data                           │
   │                                                │
   │ • Right to Erasure (Right to be Forgotten)     │
   │   DELETE /api/user/account                     │
   │   (Cascading delete + anonymization)           │
   │                                                │
   │ • Right to Data Portability                    │
   │   GET /api/user/export                         │
   │   (JSON/CSV format)                            │
   │                                                │
   │ • Right to Restrict Processing                 │
   │   POST /api/user/restrict                      │
   │                                                │
   │ • Right to Object                              │
   │   POST /api/user/object                        │
   └────────────────────────────────────────────────┘

2. Data Processing:
   • Lawful basis for processing
   • Explicit consent
   • Purpose limitation
   • Data minimization
   • Accuracy
   • Storage limitation
   • Integrity and confidentiality

3. Data Protection by Design:
   • Privacy by default
   • Pseudonymization
   • Encryption
   • Access controls
   • Audit logging
```

### PCI DSS対応 / PCI DSS Compliance

```
┌──────────────────────────────────────────────────────────────────┐
│              PCI DSS Compliance (Payment Card Industry)          │
└──────────────────────────────────────────────────────────────────┘

Requirements:
1. Build and Maintain a Secure Network
   ✅ Firewall configuration
   ✅ Default passwords changed

2. Protect Cardholder Data
   ✅ Encrypted storage
   ✅ Encrypted transmission
   ✅ Tokenization (Stripe, PayPal)

3. Maintain a Vulnerability Management Program
   ✅ Antivirus software
   ✅ Security patches
   ✅ Regular updates

4. Implement Strong Access Control Measures
   ✅ Need-to-know basis
   ✅ Unique IDs
   ✅ Physical access control

5. Regularly Monitor and Test Networks
   ✅ Access logs
   ✅ Security testing
   ✅ Penetration testing

6. Maintain an Information Security Policy
   ✅ Security policy
   ✅ Staff training
   ✅ Incident response plan

Implementation:
• Use payment gateway (Stripe)
• Never store CVV
• Tokenize card numbers
• PCI SAQ (Self-Assessment Questionnaire)
• Annual compliance review
```

---

## 🔍 セキュリティ監査 / Security Auditing

### 監査ログ / Audit Logs

```
┌──────────────────────────────────────────────────────────────────┐
│              Audit Logging System                                │
└──────────────────────────────────────────────────────────────────┘

Logged Events:
┌────────────────────────────────────────────────────────────────┐
│ Event Type          │ Details                                  │
├────────────────────────────────────────────────────────────────┤
│ Authentication      │ Login, Logout, MFA                       │
│ Authorization       │ Permission checks, Role changes          │
│ Data Access         │ Read, Create, Update, Delete             │
│ Configuration       │ Settings changes, Admin actions          │
│ Security            │ Failed auth, Suspicious activity         │
└────────────────────────────────────────────────────────────────┘

Log Format (JSON):
{
  "timestamp": "2025-12-03T10:30:00.123Z",
  "event_id": "evt_123456",
  "event_type": "data.access",
  "actor": {
    "user_id": "user_abc",
    "ip_address": "192.168.1.100",
    "user_agent": "Mozilla/5.0..."
  },
  "resource": {
    "type": "address",
    "id": "addr_xyz",
    "action": "read"
  },
  "result": "success",
  "metadata": {
    "request_id": "req_789",
    "api_version": "v1"
  }
}

Retention:
• Hot storage: 90 days (Elasticsearch)
• Warm storage: 1 year (S3)
• Cold storage: 7 years (Glacier)
```

---

## 関連ドキュメント / Related Documentation

- [システム全体図](./system-overview.md)
- [データフロー図](./data-flows.md)
- [ユーザージャーニー](./user-journeys.md)
- [技術統合図](./technical-integration.md)

---

**最終更新 / Last Updated**: 2025-12-03
